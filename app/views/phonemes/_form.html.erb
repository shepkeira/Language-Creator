<%= form_with(model: [@language, phoneme]) do |form| %>
  <% if phoneme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(phoneme.errors.count, "error") %> prohibited this word from being saved:</h2>

      <ul>
        <% phoneme.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :english_word %>
    <%= form.text_area :english_word %>
  </div>

  <div class="field">
    <%= form.label :language_word %>
    <%= form.text_area :language_word %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>